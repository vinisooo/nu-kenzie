

const LandingPage = ({enter})=>{

    return(
        <main>
            <h1>Não entrou</h1>
            <button onClick={()=>enter()}></button>
        </main>
    )

}

export default LandingPage